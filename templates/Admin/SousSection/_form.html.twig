{{ form_start(form) }}
<div class="form-group">
    {{ form_row(form.nom) }}
</div>
<div class="form-group">
    {{ form_row(form.section) }}
</div>
<button class="btn btn-outline-primary mt-3">Enregistrer</button>
{{ form_end(form) }}